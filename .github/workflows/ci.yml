name: "CI"

on:
  push:
    branches:
      - "master"

jobs:
  ci:
    name: "CI"
    runs-on: ubuntu-latest
    steps:
      - name: Check out Git repository
        uses: actions/checkout@v3

      - name: "See what's in the folder"
        run: "ls"
